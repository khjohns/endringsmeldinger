<span class="subsidiary-badge">Subsidiært</span>

<style>
  .subsidiary-badge {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    padding: 5px 8px;
    font-family: var(--font-mono, var(--font-data));
    font-size: 10px;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--green, var(--color-vekt));
    background: color-mix(in srgb, var(--green-bg, var(--color-vekt-bg)) 55%, transparent);
    border: 1px dashed var(--green, var(--color-vekt));
    border-radius: 6px;
  }
</style>
