# ==============================================================================
# KOE Automation System - Development Dependencies
# ==============================================================================
#
# Installer med: pip install -r requirements.txt -r requirements-dev.txt
#
# ==============================================================================

# ------------------------------------------------------------------------------
# Testing
# ------------------------------------------------------------------------------
pytest>=7.0.0                     # Test framework
pytest-cov>=4.0.0                 # Code coverage
pytest-mock>=3.10.0               # Mocking support

# ------------------------------------------------------------------------------
# Code Quality
# ------------------------------------------------------------------------------
# Pinnet 2026-09-20: ruff gater i CI, og >= betyr at en ny utgivelse kan
# aktivere regler innenfor de valgte familiene (E, F, B, I, UP, SIM) og gjøre
# hver PR rød uten at noen har endret kode. Oppgradering skal være en bevisst
# handling med en runde rydding, ikke noe som skjer av seg selv.
ruff==0.16.8                      # Fast linter + formatter (replaces flake8, black, isort)
mypy>=1.0.0                       # Type checker

# ------------------------------------------------------------------------------
# Additional Testing Tools
# ------------------------------------------------------------------------------
pytest-asyncio>=0.21.0            # Async testing support
faker>=20.0.0                     # Fake data generation for tests
